[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vt-vut-validation"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
vt-vut-validate = "vut_validation.cli:entrypoint"

[tool.setuptools.packages.find]
where = ["src"]
